export class Pin {

  constructor(public id: string, public author: string, public text: string, public imageUrl: string, public userId: string, public savedBy: string[], public likedBy: string[]) {
  }

}
